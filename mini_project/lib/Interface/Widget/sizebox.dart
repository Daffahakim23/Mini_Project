import 'package:flutter/material.dart';

class MySizeBox extends SizedBox {
  const MySizeBox({Key? key, double width = 0.0, double height = 0.0})
      : super(key: key, width: width, height: height);
}
